'use strict';

/**
 * tasdiiq-doc-auth-complete router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::tasdiiq-doc-auth-complete.tasdiiq-doc-auth-complete');
