'use strict';

/**
 * tasdiiq-doc-auth-complete controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::tasdiiq-doc-auth-complete.tasdiiq-doc-auth-complete');
